
class ATM:

    def __init__(self, initial_balance=1000, default_pin="1234"):
        self.balance = initial_balance
        self.pin = default_pin

    def check_balance(self_balance):
        print(f"your current balance is : ${check_balance()}")

    def withdraw(account_balanse, amount):
        if amount <= 0:
            return "Invelaid withdrawal amount , pleas enter positive amount"
        elif amount > account_balanse:
            return "insufficient funds"
        else:
            new_balance = account_balanse - amount
            return new_balance

        def deposit(amount):
            if amount <= 0:

        return "Invelaid deposit amount , pleas enter positive amount"
        else:
        new_balance = deposit_balanse + amount

    return new_balance


def verify_pin(self, input_pin):
    input_pin = int(input("inter your password:"))
    if input_pin == self.pin:
        return "welcome"
    else:
        return "incorrect pin"


def change_pin(old_pin, new_pin):
    input_pin = int(input("inter your password:"))


if input_pin != self.pin
    print("your pin is not correcet")
return
new_pin = int(input("enter new pin"))
confirm_pin = int(input("rewrite the pin"))
if new_pin != confirm_pin:
    print("")

